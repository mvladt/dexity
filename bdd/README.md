# BDD

Тест-сценарии Dexity в Gherkin'е. Любая область — auth, история, настройки, web search, LLM-поведение и т.д.

Используются как ТЗ — для ручного прогона, для агента-тестера через `playwright-cli`, или для генерации `playwright`-тестов.

Раннера пока нет.
