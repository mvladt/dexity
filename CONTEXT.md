# Текущий контекст

## Чем занимались

Фича **#15 «Web search + цитаты»** из `docs/aikit-improvements-plan.md`. План — `docs/web-search-plan.md`, результаты по этапам — `docs/web-search-result.md`.

## Где остановились

**Этапы 0–3 закрыты и закоммичены:**

- `82b517b` — этап 3 (фронт: тогл «Web», блок «Источники», цитаты)
- `5039527` — этап 2 (бэк: таблица `sources`, SSE-эвент, инжект в промпт)
- `0edca08` — этап 1 (`server/src/services/search.ts` через Yandex Search API v2)

Браузерная проверка прошла: тогл включается, источники появляются до текста, цитата `[1]` рендерится как линк на `#src-{messageId}-1`, клик скроллит к якорю, после reload всё подтягивается из БД.

## Что осталось — Этап 4

Из чек-листа в `docs/web-search-plan.md` (раздел 7):

- [ ] CSS-полировка `SourcesBlock` (компактные карточки, hover на ссылках, mobile-first). Минимальный CSS уже написан на этапе 3 — нужно ревизия и доводка.
- [ ] Smoke E2E (Playwright): включить тогл «Web», задать вопрос, дождаться блока «Источники», кликнуть `[1]`, проверить scroll к якорю.

После этапа 4 — обновить спеки (раздел 6 плана: `specs/overview.md`, `server/specs/backend.md`, `client/specs/frontend.md`).

## Окружение

- Dev-сервера: client на :5173 (`cd client && npm run dev`), server на :3001 (`cd server && npm run dev`). В новой сессии вероятно понадобится поднять их заново.
- `YC_SEARCH_API_KEY` уже в `server/.env`.
- Memory актуальна: Sonnet для кодерских субагентов, агенты не коммитят, дев-серверы.
