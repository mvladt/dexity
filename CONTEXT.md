# Context

Последнее обновление: 2026-05-08

## Текущее состояние

MVP полностью работоспособен и протестирован:
- Логин, создание чатов, стриминг SSE — всё работает
- Рендер markdown, подсветка синтаксиса, копирование кода — ОК
- Все найденные баги исправлены
- Консоль браузера чистая (предупреждения от `@gravity-ui/aikit` — на совести библиотеки)
- Playwright MCP настроен, output идёт в `.playwright-mcp/output/` (в gitignore)
- Remote: `git@github.com:mvladt/dexity.git`

## История

- SDD-ревью, исправление и реорганизация спек (`specs/`, `server/specs/`, `client/specs/`)
- Написан весь код MVP (бэкенд + фронтенд + деплой)
- E2E-тестирование через Playwright MCP, найдены и исправлены баги (`docs/e2e-bugreport-2026-05-07.md`)
- Настроен агент `qa-tester` (`.claude/agents/qa-tester.md`)
- Исправлен скролл документа: `overflow: hidden` на `html, body, #root` (`docs/bug-report-scroll-bottom.md`)

## Следующий шаг

Приложение готово к деплою. Возможные направления:
- Задеплоить на сервер (nginx + systemd, см. `deploy/`)
- Написать автоматизированные E2E-тесты (Playwright)
- Новые фичи (поиск по чатам, удаление, экспорт и т.п.)
