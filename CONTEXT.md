# Context

Последнее обновление: 2026-05-08 (сессия)

## Что было сделано

- Проведено SDD-ревью `docs/dexity-spec.md` — найдено 5 критических проблем и 7 существенных замечаний
- Исправлена спека по результатам ревью
- Реорганизована структура спек: `specs/overview.md`, `server/specs/backend.md`, `client/specs/frontend.md`
- **Написан весь код MVP** (бэкенд + фронтенд + деплой)
- Настроен MCP-сервер Playwright, добавлен агент QA-тестировщика
- **Проведено E2E-тестирование через Playwright MCP** (`docs/e2e-bugreport-2026-05-07.md`):
  - BUG-001 [CRITICAL] — нет поля ввода на `/chat` → **исправлено**
  - BUG-002 [MEDIUM] — горизонтальный скролл в сайдбаре → **исправлено**
  - BUG-003 [LOW] — placeholder поля ввода на английском → **исправлено**
  - BUG-004 [INFO] — консольные предупреждения от `@gravity-ui/aikit` (проблема библиотеки, не приложения) → **не трогали**
- Исправлен отсутствующий CORS-заголовок в SSE-ответе
- Устранены предупреждения консоли браузера (React Router future flags, favicon, autocomplete)
- `.claude/` убран из репозитория, добавлен в `.gitignore`
- **Сессия 2026-05-08:**
  - Исправлен баг двойного скролла документа: `overflow: hidden` добавлен на `html, body, #root` в `client/src/styles.css` (`docs/bug-report-scroll-bottom.md`)
  - Настроен `--output-dir .playwright-mcp/output` для Playwright MCP (`.mcp.json`) — выходные файлы не попадают в репозиторий
  - Обновлены инструкции агента `qa-tester.md` (путь к output)

## Текущее состояние

MVP полностью работоспособен и протестирован:
- Логин, создание чатов, стриминг SSE — всё работает
- Рендер markdown, подсветка синтаксиса, копирование кода — ОК
- Все найденные баги исправлены
- Консоль браузера чистая (предупреждения от `@gravity-ui/aikit` — на совести библиотеки)
- Playwright MCP настроен, скриншоты и output идут в `.playwright-mcp/output/` (gitignore)

## Следующий шаг

Приложение готово к деплою. Возможные направления:
- Написать автоматизированные E2E-тесты (Playwright)
- Задеплоить на сервер (nginx + systemd, см. `deploy/`)
- Новые фичи (поиск по чатам, удаление, экспорт и т.п.)
