# Iter 2 — Проверка фикса удаления чата

## Сценарий 8 (неактивный чат)
- Статус: PASS
- Что произошло: созданы 2 чата через API, открыт chat2 (активный). Наведение курсора
  на chat1 выявило кнопку-корзину. После клика — DELETE-запрос ушёл с кодом 200,
  chat1 исчез из сайдбара, URL и активный chat2 остались без изменений.

## Сценарий 9 (активный чат)
- Статус: PASS
- Что произошло: открыт chat2 как активный. После клика на корзину — DELETE-запрос
  ушёл с кодом 200, chat2 исчез из сайдбара, произошёл редирект на `/chat`
  (активный чат сброшен).

## Сетевые запросы DELETE

| URL                                      | Статус | Тело ответа   |
| ---------------------------------------- | ------ | ------------- |
| `http://localhost:3001/api/chats/22`     | 200    | `{"ok":true}` |
| `http://localhost:3001/api/chats/23`     | 200    | `{"ok":true}` |

Заголовок `Content-Type: application/json` в DELETE-запросах **отсутствует** —
фикс в `getHeaders(false)` сработал корректно.

## Console-ошибки

Ошибок уровня `console.error` / `pageerror` нет.

Присутствуют многочисленные `[warning]` о externalized-модулях (`process`, `path`,
`fs`, `url`, `source-map-js`) — это артефакт dev-режима Vite / `@gravity-ui/aikit`,
не связанный с функциональностью удаления.

## Вердикт

PASS — фикс работает. DELETE-запросы уходят без `Content-Type: application/json`,
сервер возвращает 200, UI обновляется корректно: удалённый чат пропадает из сайдбара,
при удалении активного чата — редирект на `/chat`.
